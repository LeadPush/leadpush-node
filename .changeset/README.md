# Changesets

This directory stores release notes and version bumps for `@leadpush/sdk-node`.

For user-facing changes, run:

```sh
pnpm changeset
```

Choose the appropriate semver bump and commit the generated markdown file with the change.
